// Reuses the existing datadog-demo Macbook chrome so all partner demos
// stay visually consistent. If we ever extract a shared frame, this file
// becomes the single touch-point to flip the import.
export { MacbookFrame } from '@/components/datadog-demo/artifacts/macbook-frame';
